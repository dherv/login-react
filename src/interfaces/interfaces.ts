export interface LoginValues {
  firstName: string;
  lastName: string;
  email: string;
}

export interface RegisterValues {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
}
