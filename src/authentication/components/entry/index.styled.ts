import styled from "styled-components";
import { Form } from "formik";

export const SC = {
  Form: styled(Form)`
    display: flex;
    flex-direction: column;
  `,
  Input: styled.input``,
  Label: styled.label``
};
